using System;

namespace Library
{
         public class Image : Element
         {
                  public Image(string name, int size, int posicionX, int posicionY, Screen screen)
                  : base(name, size, posicionX, posicionY, screen)
                  {
                  }
         }
}