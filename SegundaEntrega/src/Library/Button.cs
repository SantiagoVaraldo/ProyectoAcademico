using System;

namespace Library
{
         public class Button : Element
         {
                  public Button(string name, int size, int posicionX, int posicionY, Screen screen)
                  : base(name, size, posicionX, posicionY, screen)
                  {
                  }
         }
}
